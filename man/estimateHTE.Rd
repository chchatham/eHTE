% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eHTE.R
\name{estimateHTE}
\alias{estimateHTE}
\title{estimateHTE}
\usage{
estimateHTE(
  placebo_arm,
  drug_arm,
  pctiles = seq(from = 3, to = 97, by = 2)/100
)
}
\arguments{
\item{placebo_arm}{A vector of patient-level treatment effects from the control condition (e.g., placebo).}

\item{drug_arm}{A vector of patient-level treatment effects from the active condition (e.g., active drug).}

\item{pctiles}{A vector of percentiles at which to evaluate the drug/placebo difference. Default is a vector of percentiles between the 3rd and 97th at increments of 2 (as in Siegel et al 2024).}
}
\value{
A custom object of class 'eHTE_class', which accepts methods summary(), plot(), and print(). P-value will be null until testHTE() is run on the result.
}
\description{
Estimates the eHTE test statistic for two samples of possibly uneven length.
}
\examples{
# Basic Uage
result <- estimateHTE(placebo_arm=rnorm(100,mean=0,sd=1),drug_arm=rnorm(120,mean=.5,sd=1.5))
plot(result)
summary(result)
}
